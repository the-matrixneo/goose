# coding: utf-8

"""
    goose-server

    An AI agent

    The version of the OpenAPI document: 1.9.0
    Contact: ai-oss-tools@block.xyz
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from goose_api.models.raw_audio_content import RawAudioContent
from goose_api.models.raw_embedded_resource import RawEmbeddedResource
from goose_api.models.raw_image_content import RawImageContent
from goose_api.models.raw_resource import RawResource
from goose_api.models.raw_text_content import RawTextContent
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

CONTENT_ONE_OF_SCHEMAS = ["RawAudioContent", "RawEmbeddedResource", "RawImageContent", "RawResource", "RawTextContent"]

class Content(BaseModel):
    """
    Content
    """
    # data type: RawTextContent
    oneof_schema_1_validator: Optional[RawTextContent] = None
    # data type: RawImageContent
    oneof_schema_2_validator: Optional[RawImageContent] = None
    # data type: RawEmbeddedResource
    oneof_schema_3_validator: Optional[RawEmbeddedResource] = None
    # data type: RawAudioContent
    oneof_schema_4_validator: Optional[RawAudioContent] = None
    # data type: RawResource
    oneof_schema_5_validator: Optional[RawResource] = None
    actual_instance: Optional[Union[RawAudioContent, RawEmbeddedResource, RawImageContent, RawResource, RawTextContent]] = None
    one_of_schemas: Set[str] = { "RawAudioContent", "RawEmbeddedResource", "RawImageContent", "RawResource", "RawTextContent" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = Content.model_construct()
        error_messages = []
        match = 0
        # validate data type: RawTextContent
        if not isinstance(v, RawTextContent):
            error_messages.append(f"Error! Input type `{type(v)}` is not `RawTextContent`")
        else:
            match += 1
        # validate data type: RawImageContent
        if not isinstance(v, RawImageContent):
            error_messages.append(f"Error! Input type `{type(v)}` is not `RawImageContent`")
        else:
            match += 1
        # validate data type: RawEmbeddedResource
        if not isinstance(v, RawEmbeddedResource):
            error_messages.append(f"Error! Input type `{type(v)}` is not `RawEmbeddedResource`")
        else:
            match += 1
        # validate data type: RawAudioContent
        if not isinstance(v, RawAudioContent):
            error_messages.append(f"Error! Input type `{type(v)}` is not `RawAudioContent`")
        else:
            match += 1
        # validate data type: RawResource
        if not isinstance(v, RawResource):
            error_messages.append(f"Error! Input type `{type(v)}` is not `RawResource`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in Content with oneOf schemas: RawAudioContent, RawEmbeddedResource, RawImageContent, RawResource, RawTextContent. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in Content with oneOf schemas: RawAudioContent, RawEmbeddedResource, RawImageContent, RawResource, RawTextContent. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into RawTextContent
        try:
            instance.actual_instance = RawTextContent.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into RawImageContent
        try:
            instance.actual_instance = RawImageContent.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into RawEmbeddedResource
        try:
            instance.actual_instance = RawEmbeddedResource.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into RawAudioContent
        try:
            instance.actual_instance = RawAudioContent.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into RawResource
        try:
            instance.actual_instance = RawResource.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into Content with oneOf schemas: RawAudioContent, RawEmbeddedResource, RawImageContent, RawResource, RawTextContent. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into Content with oneOf schemas: RawAudioContent, RawEmbeddedResource, RawImageContent, RawResource, RawTextContent. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], RawAudioContent, RawEmbeddedResource, RawImageContent, RawResource, RawTextContent]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


