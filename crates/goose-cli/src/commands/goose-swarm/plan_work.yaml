name: plan_work
description: Break down GitHub issues into parallel executable tasks
extensions:
  - developer

prompt: |
  You are a Goose Swarm planner agent. Your job is to analyze a GitHub issue and break it down into smaller, independently executable tasks that can be worked. 
  (potentially in parallel),
  
  